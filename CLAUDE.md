# CLAUDE.md — issue2content

핫이슈를 X 포스트로 만드는 로컬 파이프라인. 글은 API가 아니라 이 세션이 직접 쓴다.

## 운영 방식

Windows 작업 스케줄러가 `auto_round.ps1`을 09:07부터 매시 실행한다(17:07까지).
스크립트는 **새 claude 세션**을 띄워 `/x-blog-round`를 돌린다.

`/loop`로 되돌리지 말 것. 같은 세션에 회차를 쌓으면 API 호출마다 대화 전체를 다시 읽어
(캐시 읽기가 토큰의 97%였다) 회차마다 비용이 커진다. 새 세션이면 회차당 약 12M → 4.4M다.
대화형으로 여러 회차를 돌릴 때도 회차 사이에 `/clear`를 쓴다.

**한 회차 = 이슈 1건 = X 단독 포스트 1개.**

## 글 품질 관련 작업 시 반드시 먼저 읽을 것

- `docs/ai-patterns-checklist.md` — 한국어 AI 문체 30항목 (A: AI패턴, B: 번역투, C: 가독성)
- `docs/lint-protocol.md` — 린트 실행 명세 (**현재는 2회로 축약해 운영한다.** 산출물이
  포스트 하나뿐이라 3회는 과했다. 회차별 입력·합격선·금지사항은 이 문서를 따른다)
- `docs/copyright-checklist.md` — 저작권 15항목 (초안 작성 원칙으로 선적용)

## 절대 규칙

- **사실·수치·출처를 창작하지 않는다.** 원문에서 확인한 것만 쓴다. 출처 간 수치가 엇갈리고
  어느 쪽도 확신할 수 없으면 아예 쓰지 않는다
- **"얼마나 대단한지"도 조사해서 쓴다.** '사상 처음', '역대 최대' 같은 평가는 근거를 찾아
  확인한 뒤에만 쓴다. 근거를 못 찾으면 대단하다고 쓰지 말고 사실만 쓴다
- **작성자의 고찰이 읽혀야 한다.** 수치를 나열하는 보도자료 톤은 실패다. 다만 근거 없는
  감상·과장은 금지다 — 확인된 사실이 스스로 무게를 갖게 배치한다
- **지난 이슈를 쓰지 않는다.** 최신 기사 6시간 초과면 그 회차는 건너뛴다
- **지수·시장 전체 흐름을 쓰지 않는다.** 코스피 등락·사이드카·환율은 증권사 앱만 켜도
  보인다. 개별 종목·기업·제품·임상 결과 같은 특정 사건이어야 한다
- **X 글자수는 가중 계산으로 검증한다.** 한글·CJK는 1자당 2로 세므로 280 한도는 한글 기준
  실질 140자다. 자수로 세면 반드시 초과한다
- AI 탐지 회피 목적의 인위적 변형(오탈자 삽입 등)은 하지 않는다
- **AI 이미지를 생성하지 않는다.** 이미지는 cards.py의 코드 렌더 카드만 쓴다

## 만들지 않는 것

블로그 글, 첫 답글, 스레드, 카드 이미지, 업로드 전 체크리스트.
2026-08-20에 전부 잘라냈다 — 과했고, 정작 X 본문 품질이 뒤로 밀렸다.

## 전략

`전략.md` — X 팔로워 성장 전략. 매 회차 참조한다.
